# CLAUDE.md — Pulsar Network site (pulsarnetwork.xyz)

> Handoff context for Claude Code working in this repo. The **source of truth for ops is the
> OKFs**, especially `OKF/xandeum/pulsar-dashboard-ops.md` (full runbook). Keep the OKFs updated
> after any meaningful change — they are the bridge across Cowork, Claude Code, and the owner.
> **Follow the OKF Governance Policy in `OKF/xandeum/okf-system.md`:** read OKF → validate → execute → verify → update when material → record evidence → complete. Do NOT assume the newest file/edit is automatically correct; verify against the canonical files. Memory = pointers only. Sensitive files never go in Git.

## ⚠️ Open operator tasks (read these FIRST, before any fleet/site work)
Task briefs live in the working folder `Xandeum Nodes/`. Read in order:
1. `claude-code-task-correct-fleet-model.md` — a STALE fleet model ("all devnet", "HA pair = 2 machines") was recently in play and may have leaked into this file / shared context. This brief has the VERIFIED topology; trust it over any older summary.
2. `claude-code-task-backup-node-keys.md` — HIGHEST PRIORITY: only 1 of 12 pNode private keys has an off-server copy. Back up all 12.
3. `claude-code-task-fleet-audit.md` — read-only version + cluster audit per machine.
4. `claude-code-task-data-pull.md` — remaining gap is storage; stake/yuga are already decoded on-chain.

Verified fleet truth: 6 licenses = devnet+mainnet pairs = 12 machines; Hamza = Pairs 1/2/3, Abdou = Pairs 4/5/6; mainnet pods pinned 1.3.3 (`apt-mark hold`), devnet on 1.4.x; the Trust page lists the 6 MAINNET pubkeys and they are CORRECT.

## What this is
A **static** site on **GitHub Pages** (repo `PhoenixAB88/pulsar-network`, main/root) at the custom
domain **pulsarnetwork.xyz**. It's a live, real-time monitor of the Xandeum pNode network plus an
Insights article section. "Static site, live data": the page is static HTML; the live numbers come
from small JS data files regenerated by cron on two generator nodes and committed alongside.

## Golden rules
1. **Edit `Pulsar_Network.html`** (the canonical source). Before deploy, `cp Pulsar_Network.html index.html` — GitHub Pages serves `index.html`.
2. **Never hand-edit the cron-generated data files** (they're overwritten every cycle): `network_data*.js`, `network_history*.js`, `stake_data.js`, `net_storage*.js`, `node_storage*.js`, `credits_data.js`, `node_stats_*.js`.
3. **Never commit secrets**: Helius RPC key, Telegram bot token, etc. They live in PRIVATE files / on the nodes only.
4. **Watch deploy collisions** (see below) — push when the crons aren't mid-cycle.

## Deploy (from a machine with the repo + SSH key `~/.ssh/xandeum`)
```
cp Pulsar_Network.html index.html
rsync -az --exclude='network_data*.js' --exclude='network_history*.js' --exclude='stake_data.js' --exclude='net_storage*.js' --exclude='node_storage*.js' --exclude='credits_data.js' --exclude='node_stats_*.js' -e "ssh -i ~/.ssh/xandeum" ./ root@62.171.187.91:/root/pulsar-network/
ssh -i ~/.ssh/xandeum root@62.171.187.91 'cd /root/pulsar-network && git add -A && git -c user.name=Pulsar -c user.email=bot@users.noreply.github.com commit -q -m "deploy" && git pull -q --rebase origin main && git push -q origin main && echo PUSHED'
```
- The node clone needed `git config --global --add safe.directory /root/pulsar-network` once.
- **Deploy collisions:** two cron generators (devnet `62.171.187.91`, mainnet `13.140.143.120`) commit+push every ~10-15 min. A manual push interleaving with a cron push can cancel the Pages build; the site stays on the last green build (new files 404) until one push completes uninterrupted. Push when idle, or just re-run.

## Cron generators (Python; live in `Xandeum Nodes/`, scp'd to the nodes)
- `pulsar_build.py` → `network_data*.js` (get-pods roster, geolocated).
- `pulsar_stake.py` → `stake_data.js` (on-chain stake via Helius, server-side; devnet node).
- `pulsar_history.py` → `network_history*.js` (rolling uptime; state file kept OUTSIDE the repo).
- `pulsar_alerts.py` → Telegram down-node alerts (mainnet node + sibling watchers).
- `pulsar_addself.py` → injects the generator's own node into get-pods (a node omits itself).
- `pulsar_netstats.py <repo> <cluster>` → `net_storage_<cluster>.js` (`window.__NS[cluster]`) + `node_storage_<cluster>.js` (`window.CX[cluster]`). Calls pRPC **`get-pods-with-stats`** on localhost, which returns the FULL gossip roster WITH per-node storage (`storage_committed`/`storage_used`). **Self-sources network-wide storage, NO Atlas needed.** Runs on BOTH generators (devnet writes `_devnet`, mainnet writes `_mainnet`; merge-safe, no clobber). Mainnet total ~356 TiB / 113 nodes; devnet ~33 TiB / 184. Discovered 2026-06-28 from XanDash `/endpoints`. **Supersedes** `pulsar_storage_direct.py` and the Mac-side `fleet-dashboard/refresh_storage.py` (both now redundant; old `net_storage.js`/`node_storage.js` orphaned, can be deleted).
- `pulsar_credits.py` → `credits_data.js` (OFFICIAL credits from podcredits.xandeum.network; flat pubkey→credits map; powers the Health column).

## Data the frontend reads (window globals)
`NETWORK_*`, `STAKE/STAKE_TOTAL/REG/CUR_YUGA`, `NODE_UPTIME*`, `NODE_HIST*`, `__NS` (net storage),
`CX` (per-node storage), `PODCREDITS` (`.creds`,`.max`), `NODE_STATS`, `XAND_PRICE/SOL_PRICE` (CoinGecko, client-side).

## pRPC + data sources
- pRPC: `POST http://<node-ip>:6000/rpc`, methods `get-pods` / `get-stats` / **`get-pods-with-stats`** (HYPHENS). **`get-pods-with-stats` on ANY node (localhost) returns the full gossip roster WITH per-node storage** — works on devnet 1.4.x AND **mainnet 1.3.3 (mainnet DOES serve pRPC**, contrary to the old note). This self-sources network-wide storage; Atlas is NOT needed for it. (Discovered 2026-06-28 from XanDash `/endpoints`.)
- Official credits API: `https://podcredits.xandeum.network/api/pods-credits` (devnet) / `/api/mainnet-pod-credits` (mainnet). Works **server-side** (cron uses it) but is **NOT CORS-open to the browser** (client `fetch` = "Failed to fetch", verified 2026-06-29). So client-side pages (calculator.html, network-stats.html) must read the **same-origin generated files** (`window.__NS`, `window.CX`, `window.STAKE`, `credits_data.js`), never `fetch()` podcredits directly. CoinGecko's price API *is* CORS-open (fine to fetch client-side).
- Do NOT depend on ChillXand (competitor; removed).

## Security
CSP via `<meta>` (GitHub Pages can't set headers; frame-ancestors/HSTS need a proxy). SRI on the
pinned Leaflet CDN. `esc()` + `b58()` sanitize gossip values before innerHTML. Key-only SSH on nodes.

## OG / social cards
`og-<slug>.png` (1200x630) are generated by `Xandeum Nodes/make_og_cards.py` (Python/PIL, premium
code-rendered — not Canva). Edit its `CARDS` list and re-run; deploy the `og-*.png`. **X posting:
text + link only, NO image attachment** (attachments kill the link card); cache-bust the URL (`?v=N`)
to force a fresh crawl.

## Where to look
- Full runbook + history: `OKF/xandeum/pulsar-dashboard-ops.md`
- Fleet/ops, monetization, nodes: `OKF/xandeum/fleet-dashboard.md`, `pulsar-monetization.md`, `xandeum-nodes.md`
- Keep these OKFs updated as you change things.
